# NgFirebase



## Installation
1. Angular CLI
   ``` 
   npm install -g @angular/cli 
   ```
1. Firebase Tools 
   ``` 
   npm install -g firebase-tools
   ```

   firebase login
   firebase projects:list
   
1. Angular CLI
   ``` 
   npm install -g @angular/cli 
   ```
1. Angular CLI
   ``` 
   npm install -g @angular/cli 
   ```

